<p id="notice"><%= notice %></p>
<script src="https://cdn.plot.ly/plotly-latest.min.js"></script>

<h2> <%= @report.name %> </h2>

<% for form_appl in @report.form_applications %>
<% for offer in form_appl.semester.offers %>
<% for prof in offer.professors %>

<h2> <%= link_to "#{form_appl.to_s}/#{offer.to_s} ---> #{prof.name}", reports_individual_url(@report.id, prof.id, offer.id) %> </h2>

<% end %>
<% end %>
<% end %>

<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>
